# Task

Write a documentation page (`docs/containerization.md`) capturing current (year 2026)
best practices for containerizing a modern `uv`-managed Python project, with Podman as the
primary tool and configuration kept Docker-compatible. The document prepares the scaffolder
for a future service backend and should cover image authoring, Podman/Docker compatibility,
security, healthchecks, and local multi-service stacks, grounded in this repo's toolset
(uv, Python 3.14, private GitLab index) and existing `docs/` conventions.
