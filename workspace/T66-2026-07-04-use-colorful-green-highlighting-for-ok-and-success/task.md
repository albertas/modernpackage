# Task

Highlight affirmative status words (such as "ok" and "success"/"passed") in
green when the CLI prints its output, so successful checkpoints stand out to the
user. Additionally, insert blank lines between the paragraphs/sections of the
package-initialization output so the terminal output is easier to scan.
