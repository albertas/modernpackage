# Task: Add test-coverage measurement and reach >= 95% coverage

Configure test-coverage measurement for the `modernpackage` package and add the
tests required to reach at least 95% line coverage, enforced automatically so the
test suite fails below that threshold. This raises confidence that the package's
CLI logic (argument parsing, validation, and package-initialisation flow) is
actually exercised by the test suite.
