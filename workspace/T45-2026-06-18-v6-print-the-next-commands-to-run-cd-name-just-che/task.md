# Task

After `modernpackage` successfully scaffolds a new package, print the next
commands the user should run to start working in it, namely `cd <name> && just
check`. This extends the existing post-init success output so the user is given
an actionable, copy-pasteable next step once scaffolding completes.
