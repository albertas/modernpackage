# Task

Research current containerization best practices online and write a reference document (in `docs/`) for the `modernpackage` scaffolder. The document targets Podman as the primary tool while keeping configuration Docker-compatible, and will later guide the scaffolder's backend feature and its FastAPI-related documentation. It should capture concrete, current guidance (image authoring, builds, running, and compatibility) suited to a modern Python project that uses `uv` and a `Justfile`.
