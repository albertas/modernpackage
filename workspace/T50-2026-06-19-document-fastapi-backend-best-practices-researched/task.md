# Task

Write a new `docs/` reference document capturing researched best practices for building a
modern FastAPI backend with SQLAlchemy (async), asyncpg, Alembic migrations, uv-based
dependency management, containerization, and dependency-injection-based testing. The document
is forward-looking guidance (no backend code is implemented) that prepares the scaffolder's
planned `--backend`/`--fastapi` feature, mirroring the style and role of the existing
`docs/containerization.md`. It must follow the established `docs/` conventions (overview.md
backlink, hub-table entry) so it integrates cleanly with the documentation set.
