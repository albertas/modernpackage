# Task

Ensure the project (and the packages it scaffolds) targets the latest stable
Python version consistently across all configuration, build, CI, and
documentation files. The aim is that every place that declares or pins a Python
version agrees on the current latest stable release, so the "bleeding edge"
toolset claim holds and there are no stale or contradictory version references.
