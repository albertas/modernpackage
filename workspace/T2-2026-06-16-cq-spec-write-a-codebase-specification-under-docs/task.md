# Task

Write a codebase specification under `docs/` that documents the `modernpackage`
project: its goals, its overall architecture, and the key code parts (the CLI
entry point, the package-initialisation/scaffolding flow, the build/tooling
configuration, and the test setup). The purpose is to give contributors and
later automated phases a single accurate reference describing what the package
does and how its pieces fit together.
