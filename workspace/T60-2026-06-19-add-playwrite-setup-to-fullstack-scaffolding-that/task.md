# Task

Add a Playwright end-to-end test setup to the `--fullstack` scaffolding so that
a generated package can test the frontend, backend, and database working
together as an integrated stack. Additionally, add a status page to the frontend
template that displays application and database health based on the output of
the backend health endpoint(s), giving generated projects a working,
demonstrable example of frontend-to-backend integration.
