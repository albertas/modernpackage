# Task

Produce a documentation file (mirroring `docs/fastapi_backend.md`) that captures
current (year 2026) best practices for a ReactJS frontend built with Vite,
automatic backend API schema synchronization, and unit testing. This document
will guide the future `--fullstack`/`--reactjs` scaffolding option so the
generated frontend follows a vetted, modern toolset rather than ad-hoc choices.
